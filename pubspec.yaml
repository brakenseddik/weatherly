name: weatherly
description: "A flutter package for fetching weather details from an API endpoint and offers a predefined widgets"
version: 0.0.1
homepage:

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.5.0+1
  flutter_svg: ^2.0.10+1
  intl: ^0.19.0
  flutter_typeahead: ^5.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mocktail: ^1.0.4


flutter:
